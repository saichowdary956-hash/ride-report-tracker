services:
  - type: web
    name: ride-report-tracker
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ride_report_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: DATABASE_URL
        sync: false
